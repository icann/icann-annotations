#A ICANN
#C RFC 9210 shows that TCP is useful for all DNS, not just zone transfers
#L 45
<p>@@RFC9210@@ requires all DNS implementations to support TCP.
In specific, it:
<br>
<pre>
updates [RFC1536] to remove the misconception that TCP is only useful
for zone transfers.
</pre>
</p>
    